<div class="size-medium position-center">

  <h2 class="subhead">Edit Candidate</h2>
  <div class="box box-medium box-center">
    <%= simple_form_for [:admin, @election, @candidate] do |f| %>
      <%= render partial: 'form', locals: {f: f} %>
    <%= f.submit %>
  <% end %>

</div>
