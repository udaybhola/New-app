<div class="size-medium position-center">

  <h2 class="subhead">Edit Poll for <%= @region_name %></h2>

  <div class="box box-medium box-center">
    <%= simple_form_for [:admin, @country_state, @poll] do |f| %>
      <%= render partial: 'form', locals: {f: f} %>
    <% end %>
  </div>
</div>
